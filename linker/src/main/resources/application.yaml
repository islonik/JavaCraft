host: http://localhost:8080/api/v1/links

linker:
  short-url:
    length: 6
    max-attempts: 64
  expiration-days: 30

spring:
  data:
    mongodb:
      database: links
      uri: mongodb://localhost:27017/?directConnection=true/links
#      uri: mongodb+srv://inikki:PASSWORD@mymongoinaws.pdq9e7w.mongodb.net/links
