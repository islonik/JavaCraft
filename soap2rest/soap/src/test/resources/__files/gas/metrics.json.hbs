[
{{#each parameters.metrics}}
  {
    "id" : {{this.id}},
    "meterId" : {{this.meterId}},
    "reading" : {{this.reading}},
    "date" : "{{this.date}}"
  }{{#unless @last}},{{/unless}}
{{/each}}
]
