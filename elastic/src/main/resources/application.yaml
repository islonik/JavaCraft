scheduler:
  enabled: true
server:
  port: "8001"
spring:
  elastic:
    cluster:
      host: "localhost"
      port: "9200"
      user: "elastic"
      pass: "FverGoe0"
      schema: "https"
      ssl:
        enabled: false
        path: "cert/http_ca.crt" # the same certificate which Kibana uses to connect to elastic search instance
springdoc:
  swagger-ui:
    # turn on alphabetical ordering in Swagger
    tagsSorter: alpha
